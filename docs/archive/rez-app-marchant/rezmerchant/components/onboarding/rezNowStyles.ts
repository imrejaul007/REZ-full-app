/**
 * rezNowStyles.ts
 * Shared StyleSheet for the REZ Now setup wizard step components.
 */

import { StyleSheet, Platform } from 'react-native';
import { Colors } from '@/constants/Colors';

export const styles = StyleSheet.create({
  dotsRow: { flexDirection: 'row', justifyContent: 'center', paddingVertical: 14, gap: 8 },
  dot: { width: 10, height: 10, borderRadius: 5 },
  dotFilled: { backgroundColor: Colors.light.primary },
  dotEmpty: { backgroundColor: Colors.light.border },

  stepContainer: { flex: 1, alignItems: 'center' },
  stepIcon: { marginBottom: 16, marginTop: 8 },
  stepTitle: {
    fontSize: 22,
    fontWeight: '700',
    color: Colors.light.textHeading,
    textAlign: 'center',
    marginBottom: 8,
  },
  stepSubtitle: {
    fontSize: 15,
    color: Colors.light.textSecondary,
    textAlign: 'center',
    marginBottom: 24,
    lineHeight: 22,
    paddingHorizontal: 8,
  },

  slugInputRow: {
    flexDirection: 'row',
    alignItems: 'center',
    borderWidth: 1.5,
    borderColor: Colors.light.border,
    borderRadius: 12,
    paddingHorizontal: 14,
    paddingVertical: Platform.OS === 'ios' ? 12 : 8,
    backgroundColor: Colors.light.background,
    width: '100%',
    marginBottom: 8,
  },
  slugPrefix: { fontSize: 14, color: Colors.light.textSecondary, fontWeight: '500' },
  slugInput: { flex: 1, fontSize: 15, color: Colors.light.text, padding: 0 },
  slugStatus: { fontSize: 13, fontWeight: '500', alignSelf: 'flex-start', marginBottom: 6 },
  previewUrl: {
    fontSize: 13,
    color: Colors.light.primary,
    fontWeight: '500',
    textAlign: 'center',
    marginBottom: 24,
    paddingHorizontal: 8,
  },

  chips: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 10,
    justifyContent: 'center',
    marginBottom: 32,
  },
  chip: {
    paddingHorizontal: 16,
    paddingVertical: 9,
    borderRadius: 20,
    borderWidth: 1.5,
    borderColor: Colors.light.border,
    backgroundColor: Colors.light.backgroundSecondary,
  },
  chipActive: { borderColor: Colors.light.primary, backgroundColor: Colors.light.primaryLight2 },
  chipText: { fontSize: 14, color: Colors.light.textSecondary, fontWeight: '500' },
  chipTextActive: { color: Colors.light.primary, fontWeight: '700' },

  card: {
    backgroundColor: Colors.light.background,
    borderRadius: 14,
    borderWidth: 1,
    borderColor: Colors.light.border,
    padding: 16,
    width: '100%',
    marginBottom: 28,
  },
  toggleRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingVertical: 12,
  },
  toggleRowBorder: { borderBottomWidth: 1, borderBottomColor: Colors.light.borderLight },
  toggleRowLeft: { flexDirection: 'row', alignItems: 'center' },
  toggleLabel: { fontSize: 15, color: Colors.light.text, fontWeight: '500' },

  fieldLabel: {
    fontSize: 13,
    color: Colors.light.textSecondary,
    fontWeight: '600',
    marginBottom: 6,
    marginTop: 10,
  },
  input: {
    borderWidth: 1,
    borderColor: Colors.light.border,
    borderRadius: 10,
    paddingHorizontal: 12,
    paddingVertical: Platform.OS === 'ios' ? 11 : 8,
    fontSize: 14,
    color: Colors.light.text,
    backgroundColor: Colors.light.backgroundSecondary,
  },

  navRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
    gap: 12,
  },
  backBtn: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    paddingVertical: 14,
    paddingHorizontal: 18,
    borderRadius: 12,
    borderWidth: 1.5,
    borderColor: Colors.light.primary,
  },
  backBtnText: { fontSize: 15, color: Colors.light.primary, fontWeight: '600' },
  nextBtn: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    backgroundColor: Colors.light.primary,
    borderRadius: 12,
    paddingVertical: 14,
    marginTop: 4,
  },
  nextBtnSingle: {
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    backgroundColor: Colors.light.primary,
    borderRadius: 12,
    paddingVertical: 14,
    marginTop: 4,
  },
  nextBtnDisabled: { opacity: 0.45 },
  nextBtnText: { color: '#fff', fontSize: 16, fontWeight: '700' },
  skipLink: { marginTop: 16, alignItems: 'center' },
  skipLinkText: {
    fontSize: 14,
    color: Colors.light.textSecondary,
    textDecorationLine: 'underline',
  },

  checkCircle: {
    width: 96,
    height: 96,
    borderRadius: 48,
    backgroundColor: Colors.light.success,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 20,
    marginTop: 8,
  },
  allSetTitle: {
    fontSize: 26,
    fontWeight: '800',
    color: Colors.light.textHeading,
    marginBottom: 8,
    textAlign: 'center',
  },
  allSetSubtitle: {
    fontSize: 15,
    color: Colors.light.textSecondary,
    textAlign: 'center',
    marginBottom: 28,
    lineHeight: 22,
  },
  urlCard: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: Colors.light.primaryLight2,
    borderRadius: 12,
    padding: 14,
    width: '100%',
    marginBottom: 4,
    gap: 10,
  },
  urlText: { flex: 1, fontSize: 14, color: Colors.light.primary, fontWeight: '600' },
  tapToCopy: {
    fontSize: 12,
    color: Colors.light.textMuted,
    alignSelf: 'flex-start',
    marginBottom: 20,
  },
  openBtn: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    borderWidth: 1.5,
    borderColor: Colors.light.primary,
    borderRadius: 12,
    paddingVertical: 12,
    paddingHorizontal: 24,
    marginBottom: 16,
    width: '100%',
    justifyContent: 'center',
  },
  openBtnText: { fontSize: 15, color: Colors.light.primary, fontWeight: '600' },
});
