# =============================================================================
# REZ GDPR Service - Render Deployment Configuration
# =============================================================================

services:
  - type: web
    name: rez-gdpr-service
    env: docker
    region: oregon
    plan: pro

    dockerfilePath: ./Dockerfile
    dockerContext: ../

    healthCheckPath: /health
    healthCheckInterval: 30
    healthCheckThreshold: 3

    autoScaling:
      minInstances: 1
      maxInstances: 4
      targetCPUPercent: 70
      targetMemoryPercent: 80

    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3005
      - key: APP_NAME
        value: rez-gdpr-service
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: S3_BUCKET
        value: rez-gdpr-exports
      - key: JWT_SECRET
        sync: false
      - key: SENTRY_DSN
        sync: false

    diskMounts:
      - mountPath: /var/log/rez
        name: rez-logs
        sizeGB: 10
      - mountPath: /app/temp
        name: gdpr-temp
        sizeGB: 5

preDeployCommand: npm run migrate:production
deploymentStrategy: rolling

notifications:
  slack:
    url: https://hooks.slack.com/services/xxx
    on: [failed, reactivated]
