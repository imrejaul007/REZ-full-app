# Vercel handles SPA routing via vercel.json rewrites.
# This catch-all was removed — it caused /assets/*.js and /assets/*.css
# to be served as text/html, breaking strict MIME checking in browsers.
